# Model weights

`config.py` loads the two detectors from this folder:

- `neuron_detector.pt`
- `landmark_detector.pt`

They are not tracked in git. Download both from the v1.1.0 release:
https://github.com/mostafabakhshi/DanaFish/releases/tag/v1.1.0
